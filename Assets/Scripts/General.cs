using UnityEngine;
using System.Collections;

public class General : MonoBehaviour {

    void OnGUI() {
        //GUI.Label(new Rect(100, 100, 100, 100), "Foo");
    }

    // Use this for initialization
    void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
